# 검증 기록 · 2026-09-18

첫 실제 GitHub Actions 실행은 **35306637868**, 소스 커밋 `d261d20daa9e42d6d536e43b4c2069eda19482db`이다.

- TypeScript 및 Vite 프로덕션 빌드 성공
- 콘텐츠·저장 스키마 검사 성공
- Chromium 회귀 테스트 7개 통과, 전체 95개 절 경로 포함
- 별도 수식 112개 파싱 성공, 신규 수식의 화면 오류 없음
- 확인 문제 46개, 리더에 연결된 기존 그림 184개
- 파일 누락·별도 수식 파싱의 검사 경고 0개
- 320, 375, 768, 1024, 1440px 테스트 화면에서 페이지 전체 가로 넘침 없음

이 숫자는 구조·표시 검사 결과이지 모든 번역과 캡션의 의미가 정확하다는 인증이 아니다.

서재 데스크톱, 읽기 데스크톱, 모바일 읽기, 밤 테마, 미분 실험 캡처를 이미지로 확인했다. 이후 한국어 문단을 단어 단위로 줄바꿈하도록 보완하고, 현재 절이 왼쪽 목차에서 숨지 않게 스크롤 처리를 추가했다. 목차 위치와 읽던 문단 복원을 회귀 테스트로 추가했다.

첫 빌드는 전체 앱 번들 크기 경고가 있었다. 최종 구성은 React·수식 렌더러·기존 장 데이터를 별도 청크로 나눈다. 캐시 구성을 개선하지만 모든 자료가 요청 시에만 지연 로드된다는 뜻은 아니다.

검증된 lockfile을 보존한 뒤 CI를 읽기 전용 권한과 npm ci로 전환했다. 이후 워크플로는 소스나 lockfile을 자동 수정하지 않는다. 최종 변경의 성공 여부는 해당 Actions 실행 결과로 확인한다.
